spring.application.name=user-service
frontend.url=http://localhost:8090

# Distinct session cookie name for user-service (stateless JWT in this service but set explicitly)
server.servlet.session.cookie.name=USERSESSIONID